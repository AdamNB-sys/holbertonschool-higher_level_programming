The ``1-my_list.py`` module
======================

Using ``MyList``
-------------------

``MyList`` from the ``1-my_list`` module:

    >>> MyList = __import__('1-my_list').MyList

Successes:

    >>> my_list = MyList()
    >>> my_list.append(1)
    >>> my_list.append(2)
    >>> my_list.append(3)
    >>> print(my_list)
    [1, 2, 3]
